import {defineStore} from 'pinia'

export const useAuthStore = defineStore({
    id: 'auth',
    state: () => ({
        response: null,
        }),
    getters: {

    },
    actions: {
        
    },
    })